#pragma once

namespace Config {
  // Number of LEDs
  const uint16_t ledCount = 65;
  // SSID name of WiFi to connect to
  const char *wifiSsid = "";
  // password of WiFi to connect to
  const char *wifiPassword = "";
};
